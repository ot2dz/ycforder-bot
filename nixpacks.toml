[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmd = 'corepack enable && corepack prepare pnpm@10.15.0 --activate && pnpm install --frozen-lockfile'

[phases.build]
cmd = 'pnpm run build'

[start]
cmd = 'pnpm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'